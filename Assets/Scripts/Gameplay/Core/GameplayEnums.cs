namespace Gameplay.Core
{
    public enum Order
    {
        None,
        Move,
        Attack
    }

    public enum AnimMovement
    {
        RotateLeft,
        RotateRight,
        Walk
    }
}
